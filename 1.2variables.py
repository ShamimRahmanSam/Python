ID = 44094                                          # evabe name id na diye x,y egulo variables rakha jeto .. monmto variable name deya jay we know
Name = "SRSam"                                      # variable emn words hole "" eta use krte hoy.,. integer numbers hole lagena uprer mtoi
Name = 'S.R.Sam'                                    # double quotes are the same as single quotes IN PYTHON
ID = 201611044094                                   # variables can even change type after they have been set like this way..  R CMNTS # ERPOR space rakha jay ek line er cmnts thakle. nicher line e gelei """#eta use krte hoy aage o pore"""
                                                    # number variables use krle 1st a 0 dile error dekhabe
print(ID, Name)                                     # ei program ta ekhanei sesh


        # Python allows you to assign values to multiple variables in one line: nicher gulo alada na likhe eksthe assign krte partam.
        # just like x, y, z = "Orange", "Banana", "Cherry"  .. and x = y = z = "Orange"  & just like ID, Name = 44094, 'SRSam'
        # And we can assign the same value to multiple variables in one line:.. but ekhane onnno difference bujhate evabe kora hoise

"""A variable can have a short name (like x and y) or a more descriptive name (age, carname, total_volume). Rules for Python variables:
         A variable name must start with a letter or the underscore character
         A variable name cannot start with a number
         A variable name can only contain alpha-numeric characters and underscores (A-z, 0-9, and _ )
        Variable names are case-sensitive (age, Age and AGE are three different variables)

#Legal variable names:
    myvar = "John"
    my_var = "John"
    _my_var = "John"
    myVar = "John"
    MYVAR = "John"
    myvar2 = "John"

#Illegal variable names:
    2myvar = "John"
    my-var = "John"
    my var = "John"
"""